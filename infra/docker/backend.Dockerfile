# infra/docker/backend.Dockerfile
# Canonical backend Dockerfile is at: backend/Dockerfile
# This file is kept for legacy infra references.
# Use backend/Dockerfile for all builds.
#
# See also: infra/railway/railway.toml

# Redirect: use backend/Dockerfile from repo root context
# Build command:
#   docker build -f backend/Dockerfile -t ai-resume-tailor-backend .
