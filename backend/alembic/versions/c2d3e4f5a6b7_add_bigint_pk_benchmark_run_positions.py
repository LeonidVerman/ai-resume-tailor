"""add bigint PK to benchmark_run_positions (additive step)

Revision ID: c2d3e4f5a6b7
Revises: b1c2d3e4f5a6
Create Date: 2026-03-26

Adds new_id BIGINT IDENTITY alongside existing String(36) PK.
benchmark_run_positions is a leaf table (no FK children).
"""

from alembic import op
import sqlalchemy as sa

revision = "c2d3e4f5a6b7"
down_revision = "b1c2d3e4f5a6"
branch_labels = None
depends_on = None


def upgrade() -> None:
    op.execute(
        "ALTER TABLE benchmark_run_positions "
        "ADD COLUMN new_id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL"
    )
    op.create_unique_constraint(
        "uq_benchmark_run_positions_new_id", "benchmark_run_positions", ["new_id"]
    )


def downgrade() -> None:
    op.drop_constraint(
        "uq_benchmark_run_positions_new_id", "benchmark_run_positions", type_="unique"
    )
    op.drop_column("benchmark_run_positions", "new_id")
