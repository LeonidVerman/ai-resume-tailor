"""add bigint PK to evaluation_runs (additive step)

Revision ID: a8b9c0d1e2f3
Revises: f7a8b9c0d1e2
Create Date: 2026-03-26

Adds new_id BIGINT IDENTITY alongside existing UUID PK.
No FK children to update (evaluation_runs is a leaf table).
"""

from alembic import op
import sqlalchemy as sa

revision = "a8b9c0d1e2f3"
down_revision = "f7a8b9c0d1e2"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # Add bigint identity column alongside existing UUID PK
    op.execute(
        "ALTER TABLE evaluation_runs "
        "ADD COLUMN new_id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL"
    )
    op.create_unique_constraint("uq_evaluation_runs_new_id", "evaluation_runs", ["new_id"])


def downgrade() -> None:
    op.drop_constraint("uq_evaluation_runs_new_id", "evaluation_runs", type_="unique")
    op.drop_column("evaluation_runs", "new_id")
