[PLANNER_REPAIR_LAYER v5.1]

You are repairing a TailoringPlan JSON.

Return corrected full JSON only.

------------------------------------------------------------
REPAIR RULES
------------------------------------------------------------

1) Fix only items mentioned in VALIDATION_ERRORS.
2) Preserve all other fields unchanged.
3) Do not invent facts.
4) Evidence quotes remain <=25 words.

------------------------------------------------------------
DOMAIN RULES
------------------------------------------------------------

Ensure presence of:
- jd_domain
- candidate_primary_domain
- domain_mismatch
- domain_translation_rule_ids (if mismatch=true)

If mismatch=true:
- Ensure at least one rule affects domain framing if evidence allows.
- Remove rule_ids that violate forbidden_phrases or evidence_requirements.

------------------------------------------------------------
VOCABULARY RULES
------------------------------------------------------------

- must_embed must be evidence-safe.
- If unsafe, demote or soften.

------------------------------------------------------------
SUMMARY DOMAIN STRATEGY
------------------------------------------------------------

If mismatch=true:
- Ensure summary positioning guidance is included.
- Emphasize transferable patterns first.
- Restrict source-domain emphasis.

------------------------------------------------------------
NARRATIVE PLAN REPAIR (MANDATORY WHEN REFERENCED)
------------------------------------------------------------

If VALIDATION_ERRORS mention narrative_plan OR required fields are missing:
Repair narrative_plan while preserving all other fields.

narrative_plan must include:
- anchor_role_id
- theme_ranked
- summary_coverage
- anchor_role_coverage
- domain_translation_binding

A) anchor_role_id repair
- Must match one of resume_strategy.experience.role_name values.
- Select using role priority rule:
  - most recent "high" priority role, else most recent "medium".
- Do not select unrelated roles unless they are high priority.

B) theme_ranked repair
- Must include 2–6 items.
- theme_id format: T1, T2, T3...
- priority must be one of: primary, secondary, supporting
- signature_terms must be 3–12 short terms each.

signature_terms repair rules:
- Prefer stable terms from JD keywords, vocabulary anchors, and leadership/architecture primitives.
- Avoid unsafe nouns.
- Avoid overly generic filler.

C) summary_coverage repair
- must_cover_theme_ids must include T1 and T2.
- should_cover_theme_ids may include T3.

D) anchor_role_coverage repair
Defaults (unless explicitly overridden by VALIDATION_ERRORS):
- first_k_bullets = 3
- top_k_themes_to_cover = 2
- min_theme_occurrences: {T1: 2, T2: 1}

E) domain_translation_binding repair
If domain_mismatch=true:
- min_total_rule_instantiations: min(2, len(domain_translation_rule_ids)) but >=1 if rule_ids non-empty
- min_instantiations_in_anchor_role: 1 if rule_ids non-empty else 0
- require_target_frame_in_anchor_role_first_k: true if rule_ids non-empty else false

If domain_mismatch=false:
- set values to 0 / false

------------------------------------------------------------
RETURN
------------------------------------------------------------

Return full corrected JSON only.