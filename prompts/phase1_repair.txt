[PLANNER_REPAIR_LAYER v5.3]

You are repairing a TailoringPlan JSON.

Return corrected full JSON only.

------------------------------------------------------------
REPAIR RULES
------------------------------------------------------------

1) Fix only items mentioned in VALIDATION_ERRORS.
2) Preserve all other fields unchanged.
3) Do not invent facts.
4) Evidence quotes remain <=25 words.

------------------------------------------------------------
NARRATIVE PLAN REPAIR
------------------------------------------------------------

If VALIDATION_ERRORS mention narrative_plan OR required fields are missing:
Repair narrative_plan while preserving all other fields.

Use same deterministic rules as planner.

------------------------------------------------------------
SKILL GRAPH REPAIR
------------------------------------------------------------

If VALIDATION_ERRORS mention skill_graph OR required fields are missing:
Repair skill_graph while preserving other fields.

Rules:
- tool/responsibility => forbidden unless explicit evidence supports can_claim_experience
- remove inferred vendor stacks unless explicitly evidenced

------------------------------------------------------------
COVER LETTER PLAN REPAIR (MANDATORY WHEN REFERENCED)
------------------------------------------------------------

If VALIDATION_ERRORS mention cover_letter_plan OR required fields are missing:
Repair cover_letter_plan while preserving other fields.

cover_letter_plan must include:
- structure_version = CL_V1_4PARA_2PROOF
- hook_theme_id
- proof_points (exactly 2: P1, P2)
- bridge_sentence_required (true if domain_mismatch else false)
- closing_guidance

Proof points repair rules:
- proof_id must be P1 and P2
- theme_id should map to T1 for P1 and T2 for P2 unless strong reason
- evidence_ref must point to an existing evidence_map item (free-text pointer ok)
- mechanism_hint and outcome_hint must be supported by evidence_map allowed_claims
- required_exact_span must be:
  - 10–160 chars
  - claim-safe
  - natural phrase that can appear verbatim in a cover letter
  - must not introduce tools not allowed
- allowed_tool_mentions must be subset of:
  - skill_graph.direct_skills
  - related_skills with allowed_usage == can_claim_experience

If a proof point cannot be supported:
- downgrade to an observable, non-metric outcome (e.g., "reduced incident frequency", "improved release reliability")
- keep mechanism concrete and safe
- never fabricate tools

------------------------------------------------------------
RETURN
------------------------------------------------------------

Return full corrected JSON only.