[PLANNER_REPAIR_LAYER v2.6]

You are repairing a TailoringPlan that failed validation.

You will receive:
- SCHEMA_ERRORS
- VALIDATION_ERRORS
- INVALID_PLAN
- CANDIDATE_PROFILE
- JOB_DESCRIPTION
- MASTER_RESUME
- MASTER_COVER_LETTER
- TASK
- CURRENT_DATE

GOAL

Produce a complete, valid v2.1 TailoringPlan JSON that fixes all errors.

If SCHEMA_ERRORS are present, rebuild the entire plan from scratch.

------------------------------------------------------------
STRICT TYPE CORRECTION RULES
------------------------------------------------------------

If any of the following fields are objects instead of arrays,
you MUST convert them to arrays:

- jd_top_themes
- evidence_map
- evidence_map[].evidence
- resume_strategy.experience
- role_repositioning_intent
- bullet_allocation_plan
- theme_priority.primary
- theme_priority.secondary
- theme_priority.supporting

Even if only one element exists, it MUST be wrapped in an array.

------------------------------------------------------------
ABSOLUTE RULES
------------------------------------------------------------

1) Do NOT invent facts.
2) Evidence quotes <= 25 words.
3) role_level must be one of: junior, mid, senior, director.
4) Return full JSON only.

------------------------------------------------------------
SCHEMA REQUIREMENTS
------------------------------------------------------------

A) jd_top_themes must be array of objects:
- theme
- why_important
- keywords (array)

B) evidence_map must be array of objects:
- theme
- evidence (array of objects)
- gaps (array)
- safe_translation (array)

Each evidence object:
- source
- location
- quote
- allowed_claims (array)

Evidence must not be strings.

C) resume_strategy.experience must be array of objects.

D) theme_priority must contain:
- primary (array)
- secondary (array)
- supporting (array)

E) evidence_saturation_rules must contain:
- primary_theme_min_evidence: 3
- secondary_theme_min_evidence: 2
- supporting_theme_min_evidence: 1

F) role_repositioning_intent must include all high-priority roles.

G) bullet_allocation_plan must include top 2 roles with at least 2 themes each.

------------------------------------------------------------
VALIDATION FIX RULES
------------------------------------------------------------

If insufficient evidence:
- Add distinct evidence items.
OR
- Demote the theme and promote one that meets minimums.

If unsupported JD-only theme:
- Translate to supported adjacent theme.
- Move original term to gaps.

------------------------------------------------------------
FINAL CHECK
------------------------------------------------------------

Before returning JSON:

- All required fields present.
- All array fields are arrays.
- No object replaces an array.
- Evidence entries are objects.
- Evidence saturation satisfied.
- role_repositioning_intent covers high-priority roles.
- bullet_allocation_plan covers top 2 roles.

Return JSON only.
No explanations.