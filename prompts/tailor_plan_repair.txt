[PLANNER_REPAIR_LAYER v2.7]

You are repairing a TailoringPlan that failed validation.

You will receive:
- SCHEMA_ERRORS
- VALIDATION_ERRORS
- INVALID_PLAN
- CANDIDATE_PROFILE
- JOB_DESCRIPTION
- MASTER_RESUME
- MASTER_COVER_LETTER
- TASK
- CURRENT_DATE

GOAL

Produce a complete, valid v2.1 TailoringPlan JSON that fixes all errors.

If SCHEMA_ERRORS are present, rebuild the entire plan from scratch.

------------------------------------------------------------
STRICT TYPE CORRECTION RULES
------------------------------------------------------------

If any of the following fields are objects instead of arrays,
you MUST convert them to arrays:

- jd_top_themes
- evidence_map
- evidence_map[].evidence
- resume_strategy.experience
- role_repositioning_intent
- bullet_allocation_plan
- theme_priority.primary
- theme_priority.secondary
- theme_priority.supporting

Even if only one element exists, it MUST be wrapped in an array.

------------------------------------------------------------
ABSOLUTE RULES
------------------------------------------------------------

1) Do NOT invent facts.
2) Evidence quotes <= 25 words.
3) role_level must be one of: junior, mid, senior, director.
4) Return full JSON only.

------------------------------------------------------------
SCHEMA REQUIREMENTS
------------------------------------------------------------

A) jd_top_themes must be array of objects:
- theme
- why_important
- keywords (array)

B) evidence_map must be array of objects:
- theme
- evidence (array of objects)
- gaps (array)
- safe_translation (array)

Each evidence object:
- source
- location
- quote
- allowed_claims (array)

Evidence must not be strings.

C) resume_strategy.experience must be array of objects.

D) theme_priority must contain:
- primary (array)
- secondary (array)
- supporting (array)

E) evidence_saturation_rules must contain:
- primary_theme_min_evidence: 3
- secondary_theme_min_evidence: 2
- supporting_theme_min_evidence: 1

F) role_repositioning_intent must include all high-priority roles.

G) bullet_allocation_plan must include top 2 roles with at least 2 themes each.

------------------------------------------------------------
THEME DEFENSIBILITY REPAIR (CRITICAL)
------------------------------------------------------------

If VALIDATION_ERRORS include any indication that evidence is weakly aligned,
or if a theme is JD-domain-specific but not supported by sources:

1) Do NOT try to "pad" evidence with unrelated quotes.
2) Remove or translate the theme to a defensible adjacent theme.
3) Put the original JD phrasing into gaps.
4) Use safe_translation to explain the mapping.
5) Rebuild evidence_map with directly aligned evidence objects.

Examples of invalid evidence mapping:
- Trading/crypto evidence does not prove asset management.
- Scalability metrics do not prove networking protocol expertise.

------------------------------------------------------------
SUPPORTED THEME TRANSLATION RULE (MANDATORY)
------------------------------------------------------------

If a JD theme is not defensible, translate it using:

1) "Cloud integration and asset management" or "asset discovery"
   -> "Backend integration architecture and extensibility patterns"

2) "Networking protocols and technologies"
   -> "High-throughput distributed systems and reliability under load"

3) "Low-code/no-code frameworks"
   -> "Reusable backend components and integration enablement"
   (Do not claim low-code/no-code)

Record original JD term in gaps.

------------------------------------------------------------
VALIDATION FIX RULES
------------------------------------------------------------

If insufficient evidence for a theme:
- Add distinct evidence items that directly align, OR
- Demote/translate the theme so it becomes defensible and evidence-minimum can be met.

If unsupported JD-only theme:
- Translate to supported adjacent theme.
- Move original term to gaps.
- Rebuild evidence accordingly.

If role_repositioning_intent missing entries:
- Add entries for all high-priority roles (exact role_name match).

If bullet_allocation_plan missing/weak:
- Ensure top 2 roles included
- Ensure each has at least 2 themes

------------------------------------------------------------
FINAL CHECK
------------------------------------------------------------

Before returning JSON:

- All required fields present.
- All array fields are arrays.
- Evidence entries are objects.
- Evidence saturation satisfied.
- Evidence aligns directly with theme (no semantic leaps).
- role_repositioning_intent covers high-priority roles.
- bullet_allocation_plan covers top 2 roles.

Return JSON only.
No explanations.