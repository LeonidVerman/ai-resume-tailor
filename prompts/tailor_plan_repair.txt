[PLANNER_REPAIR_LAYER v2.4]

You are repairing a TailoringPlan that failed validation.

You will receive:
- SCHEMA_ERRORS   (structural problems: missing keys, wrong types)
- VALIDATION_ERRORS (content problems: insufficient evidence, missing role intent, etc.)
- INVALID_PLAN    (the plan that failed; may be empty or malformed)
- CANDIDATE_PROFILE
- JOB_DESCRIPTION
- MASTER_RESUME

GOAL

Produce a complete, valid v2.1 TailoringPlan JSON that fixes all listed errors.

IMPORTANT: INVALID_PLAN may be empty, malformed, or use an old schema.
Do NOT attempt to patch it field by field.
Instead, treat it as a DRAFT that may have useful content,
but rebuild the full plan from scratch if SCHEMA_ERRORS are present.

------------------------------------------------------------
REPAIR CONTRACT
------------------------------------------------------------

1) The output MUST always be a complete v2.1 TailoringPlan.
   Never return a partial plan or just the corrected fields.

2) Fix every error listed in SCHEMA_ERRORS and VALIDATION_ERRORS.

3) Do NOT invent facts.
   All claims must be traceable to MASTER_RESUME or CANDIDATE_PROFILE.

4) Evidence quotes must be <= 25 words.

5) role_level must be exactly one of: junior, mid, senior, director.
   Do NOT output a job title.

------------------------------------------------------------
SCHEMA ERRORS (structural fix required)
------------------------------------------------------------

If SCHEMA_ERRORS is non-empty:
- The plan is structurally broken.
- Rebuild the entire plan from CANDIDATE_PROFILE, JOB_DESCRIPTION, and MASTER_RESUME.
- Do not rely on INVALID_PLAN for structure.

------------------------------------------------------------
EVIDENCE SATURATION FIX
------------------------------------------------------------

If VALIDATION_ERRORS indicate insufficient evidence for a theme:

Option A: Add additional distinct evidence items from:
  - architecture_patterns
  - scalability_reliability_patterns
  - technical_skills
  - MASTER_RESUME bullets

Option B: If sufficient evidence does not exist:
  - Demote the theme to a lower tier.
  - Promote a theme that CAN meet the minimum.
  - Update theme_priority accordingly.

You MUST NOT keep a theme in primary or secondary
if it cannot meet its minimum evidence count.

------------------------------------------------------------
UNSUPPORTED JD THEME FIX
------------------------------------------------------------

If a theme is based on unsupported JD-only concepts
(example: low-code/no-code frameworks):

You MUST:
- Remove it as primary or secondary.
- Translate to a supported adjacent theme if possible.
- Move original JD wording into gaps.
- Rebuild evidence accordingly.

------------------------------------------------------------
ROLE INTENT FIX
------------------------------------------------------------

If role_repositioning_intent is missing entries:
- Add entries for all high-priority roles.
- Role names must exactly match experience.role_name.

------------------------------------------------------------
FINAL CHECK
------------------------------------------------------------

Before returning JSON:
- All required v2.1 keys are present.
- role_level is exactly one of: junior, mid, senior, director.
- All themes satisfy evidence minimum.
- Every theme appears in exactly one tier.
- All high-priority roles have repositioning intent.
- No unsupported JD-only theme remains primary or secondary.

Return JSON only.
No explanations.
